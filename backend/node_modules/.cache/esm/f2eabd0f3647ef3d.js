let express,path,mongoose,api,connectToDatabase;_364‍.w("express",[["default",["express"],function(v){express=v}]]);_364‍.w("path",[["default",["path"],function(v){path=v}]]);_364‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_364‍.w("./api",[["default",["api"],function(v){api=v}]]);_364‍.w("./util/db/db-connect",[["default",["connectToDatabase"],function(v){connectToDatabase=v}]]);



// Setup Express
const app = express();
const port = process.env.PORT || 3001;

// Setup JSON parsing for request body
app.use(express.json());

// Setup our routes.


app.use('/', api);

// Make the "public" folder available statically
app.use(express.static(path.join(__dirname, '../assets')));

// Serve up the frontend's "build" directory, if we're running in production mode.
if (process.env.NODE_ENV === 'production') {
    console.log('Running in production!');

    // Make all files in that folder public
    app.use(express.static(path.join(__dirname, '../../frontend/build')));

    // If we get any GET request we can't process using one of the server routes, serve up index.html by default.
    app.get('*', (req, res) => {
        res.sendFile(path.join(__dirname, '../../frontend/build/index.html'));
    });
}


// Start the server running. Once the server is running, the given function will be called, which will
// log a simple message to the server console. Any console.log() statements in your node.js code
// can be seen in the terminal window used to run the server.
// app.listen(port, () => console.log(`App server listening on port ${port}!`));

// Start the DB running. Then, once it's connected, start the server.
// connectToDatabase()
mongoose.connect("mongodb://localhost:27017/PawsHome", {useNewUrlParser:true})
        .then(() => app.listen(port, () => console.log(`App server listening on port ${port}!`)));